#include <stdio.h>

int main() {
    char str[] = "aaabbccccd";

    int count = 1;

    for(int i = 0; str[i]; i++) {
        count = 1;

        while(str[i] == str[i+1]) {
            count++;
            i++;
        }

        printf("%c%d", str[i], count);
    }

    return 0;
}
